export type Blog = {
    title: string
    blogHtml: string
    originalTimestamp: number
    kudosCount: string
}

export type Keys = {
    spaceheyKey: string
    napoleonKey: string
}

export type AppError = {
    message: string
}
